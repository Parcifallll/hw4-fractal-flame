# Как работает генератор фрактального пламени

## Содержание

1. [Основная идея](#основная-идея)
2. [Алгоритм Chaos Game](#алгоритм-chaos-game)
3. [Аффинные преобразования](#аффинные-преобразования)
4. [Нелинейные трансформации](#нелинейные-трансформации)
5. [Веса функций](#веса-функций)
6. [Цвет и гистограмма](#цвет-и-гистограмма)
7. [Гамма-коррекция](#гамма-коррекция)
8. [Однопоточная реализация](#однопоточная-реализация)
9. [Влияние параметров](#влияние-параметров)

---

## Основная идея

Фрактальное пламя (Fractal Flame) - это **итеративный процесс**, где мы многократно применяем случайные трансформации к точке и "рисуем" её траекторию.

**Из хаоса случайности возникает красивая структура - аттрактор.**

### Что такое аттрактор?

Аттрактор - это устойчивая структура в пространстве, к которой "притягиваются" точки при многократном применении трансформаций. Разные комбинации трансформаций создают разные аттракторы = разные фракталы!

---

## Алгоритм Chaos Game

### Шаг 1: Инициализация

- Создаём пустую гистограмму (массив пикселей)
- Берём случайную начальную точку (x, y), например (-0.5, 0.3)

### Шаг 2: Итерация (повторяется тысячи раз)

```
Для каждой итерации:
  1. Выбираем СЛУЧАЙНУЮ аффинную трансформацию из списка
  2. Применяем её к точке → получаем новые координаты
  3. Выбираем СЛУЧАЙНУЮ нелинейную трансформацию (с учётом весов)
  4. Применяем её к точке → получаем финальные координаты
  5. Если точка в границах мира - ЗАПИСЫВАЕМ её в гистограмму
```

### Шаг 3: Разогрев (Warm-up)

**Первые 20 итераций пропускаем!**

Почему? Точка должна "найти" аттрактор. В начале она может быть далеко от него, поэтому мы даём ей время "устаканиться".

### Шаг 4: Финализация

После всех итераций:
1. Нормализуем гистограмму (делим цвета на количество попаданий)
2. Применяем гамма-коррекцию (для естественной яркости)
3. Конвертируем в RGB (0-255)
4. Сохраняем в PNG

---

## Аффинные преобразования

Аффинное преобразование = **линейная трансформация + сдвиг**.

### Формулы

```
x' = a*x + b*y + c
y' = d*x + e*y + f
```

### Параметры (6 коэффициентов)

| Параметр |         Что делает          |                     Пример                     |
|----------|-----------------------------|------------------------------------------------|
| **a**    | Масштаб по X + вращение     | 1.0 = без изменений, 0.5 = сжатие в 2 раза     |
| **b**    | Сдвиг/наклон X от Y (shear) | 0.5 = наклон вправо при движении вверх         |
| **c**    | Простой сдвиг по X          | 1.0 = сдвиг на 1 единицу вправо                |
| **d**    | Сдвиг/наклон Y от X (shear) | 0.5 = наклон вверх при движении вправо         |
| **e**    | Масштаб по Y + вращение     | 1.0 = без изменений, 2.0 = растяжение в 2 раза |
| **f**    | Простой сдвиг по Y          | -1.0 = сдвиг на 1 единицу вниз                 |

### Примеры преобразований

#### Поворот на 45°

```json
{
  "a": 0.707,   // cos(45°)
  "b": -0.707,  // -sin(45°)
  "c": 0,
  "d": 0.707,   // sin(45°)
  "e": 0.707,   // cos(45°)
  "f": 0
}
```

#### Сжатие в 2 раза

```json
{
  "a": 0.5,
  "b": 0,
  "c": 0,
  "d": 0,
  "e": 0.5,
  "f": 0
}
```

#### Сдвиг вправо + вниз

```json
{
  "a": 1,
  "b": 0,
  "c": 1.0,   // сдвиг вправо
  "d": 0,
  "e": 1,
  "f": -0.5   // сдвиг вниз
}
```

### Зачем несколько аффинных трансформаций?

**На каждой итерации алгоритм СЛУЧАЙНО выбирает одну** из них. Это создаёт разнообразие - точка может "прыгать" между разными областями пространства.

**Важно:** Каждая аффинная трансформация имеет свой **цвет** (RGB). Когда точка проходит через эту трансформацию, она получает этот цвет (смешивается с текущим). Так получается цветной фрактал!

---

## Нелинейные трансформации

После аффинной применяется **нелинейная** трансформация. Она "изгибает" пространство, создавая интересные формы.

### 1. LINEAR

**Формула:** `(x, y) → (x, y)`

**Что делает:** Ничего! Точка остаётся на месте.

**Эффект:** Используется для "разбавления" других трансформаций.

---

### 2. SINUSOIDAL

**Формула:** `(x, y) → (sin(x), sin(y))`

**Что делает:** Применяет синус к координатам.

**Эффект:** Создаёт волнистые, плавные формы. Прямые линии превращаются в синусоиды.

---

### 3. SPHERICAL

**Формула:** `(x, y) → (x/r², y/r²)` где `r² = x² + y²`

**Что делает:** "Выворачивает" пространство наизнанку.

**Эффект:**
- Точки **близко к центру** отбрасываются **далеко**
- Точки **далеко от центра** притягиваются **к центру**
- Создаёт сферические искажения

---

### 4. SWIRL

**Формула:** `(x, y) → (x·sin(r²) - y·cos(r²), x·cos(r²) + y·sin(r²))`

**Что делает:** Закручивает точки вокруг начала координат.

**Эффект:**
- Чем дальше от центра, тем сильнее закрутка
- Создаёт вихревые, спиралевидные паттерны
- Один из самых эффектных!

---

### 5. HORSESHOE

**Формула:** `(x, y) → ((x-y)(x+y)/r, 2xy/r)` где `r = √(x²+y²)`

**Что делает:** Создаёт подковообразное искажение.

**Эффект:** Характерная форма подковы, сжатие и растяжение.

---

### 6. POLAR

**Формула:** `(x, y) → (θ/π, r-1)` где `θ = atan2(y,x)`, `r = √(x²+y²)`

**Что делает:** Переводит из декартовых координат в полярные.

**Эффект:**
- Прямоугольники превращаются в окружности
- Создаёт радиальные паттерны

---

### 7. DISC

**Формула:** `(x, y) → (θ/π · sin(πr), θ/π · cos(πr))`

**Что делает:** Создаёт дисковые искажения.

**Эффект:** Окружности, диски, кольца.

---

### 8. HEART

**Формула:** `(x, y) → (r·sin(θr), -r·cos(θr))`

**Что делает:** Создаёт сердцевидные формы.

**Эффект:** Органические, сердцевидные паттерны.

---

### 9. SPIRAL

**Формула:** `(x, y) → ((cos(θ) + sin(r))/r, (sin(θ) - cos(r))/r)`

**Что делает:** Закручивает в спираль.

**Эффект:**
- Спиральные галактические паттерны
- Затухающие вихри
- Очень эффектно в комбинации с другими

---

### 10. HYPERBOLIC

**Формула:** `(x, y) → (sin(θ)/r, r·cos(θ))`

**Что делает:** Гиперболическое искажение пространства.

**Эффект:** Острые, вытянутые формы, гиперболы.

---

## Веса функций

В конфигурации каждая функция имеет **вес** (weight):

```json
{
  "name": "swirl",
  "weight": 3.0
},
{
  "name": "sinusoidal",
  "weight": 1.0
},
{
  "name": "spherical",
  "weight": 0.5
}
```

### Как работают веса?

**Weight = вероятность выбора функции.**

1. **Суммируем все веса:** 3.0 + 1.0 + 0.5 = 4.5

2. **Считаем вероятности:**

   - swirl: 3.0 / 4.5 = 66.7%
   - sinusoidal: 1.0 / 4.5 = 22.2%
   - spherical: 0.5 / 4.5 = 11.1%
3. **На каждой итерации:**
   - Генерируем случайное число от 0 до 4.5
   - Если 0-3.0 → swirl
   - Если 3.0-4.0 → sinusoidal
   - Если 4.0-4.5 → spherical

### Влияние весов

- **Большой вес** (2.0-3.0) → функция доминирует, её эффект сильно заметен
- **Средний вес** (0.8-1.5) → функция сбалансирована
- **Малый вес** (0.3-0.7) → функция добавляет "приправу", тонкие детали

**Совет:** Экспериментируй с весами! Они сильно влияют на итоговый вид фрактала.

---

## Цвет и гистограмма

### Как накапливается цвет?

Каждый пиксель изображения - это **счётчик** + **накопитель цвета**:

```java
class Pixel {
    int hitCount;      // сколько раз сюда попала точка
    double r, g, b;    // суммарный цвет всех точек
}
```

### На каждой итерации:

```java
// Точка попала в пиксель (500, 300)
// У точки цвет: r=200, g=100, b=150

pixel.hit(200, 100, 150);

// Внутри:
hitCount++;           // hitCount = 1
r += 200;            // r = 200
g += 100;            // g = 100
b += 150;            // b = 150
```

### После 100 попаданий:

```
hitCount = 100
r = 15000  (суммарный красный)
g = 8000   (суммарный зелёный)
b = 20000  (суммарный синий)
```

### При сохранении нормализуем:

```java
// Средний цвет точки в этом пикселе
normalR = 15000 / 100 = 150
normalG = 8000 / 100 = 80
normalB = 20000 / 100 = 200
```

### Откуда берётся цвет у точки?

**От аффинных трансформаций!** Каждая аффинная имеет свой RGB:

```java
// В коде генерируем случайный цвет для каждой аффинной
int red = random.nextInt(256);
int green = random.nextInt(256);
int blue = random.nextInt(256);

// При применении трансформации цвет смешивается
int newR = (point.r() + affine.red) / 2;  // среднее
```

**Чем больше разных аффинных** → тем больше цветовое разнообразие!

---

## Гамма-коррекция

### Зачем нужна?

Гистограмма даёт нам **количество попаданий** в каждый пиксель. Но если просто взять `hits / maxHits`, яркость будет неестественной - слишком тёмной.

**Человеческий глаз** воспринимает яркость нелинейно. Гамма-коррекция это исправляет.

### Логарифмическая гамма-коррекция

```java
// 1. Нормализуем по максимуму
brightness = log10(hitCount) / log10(maxHits)

// 2. Применяем гамму (обычно 2.2)
correctedBrightness = brightness ^ (1.0 / 2.2)

// 3. Умножаем на цвет
finalColor = normalizedColor * correctedBrightness
```

### Пример:

```
Пиксель с 100 попаданиями, максимум 10000:

Без гаммы:
  brightness = 100 / 10000 = 0.01  (очень темно!)

С гаммой:
  brightness = log10(100) / log10(10000) = 2 / 4 = 0.5
  corrected = 0.5 ^ (1/2.2) = 0.73  (светлее!)
```

**Результат:** Изображение выглядит естественнее, с плавными переходами яркости.

---

## Однопоточная реализация

### Псевдокод:

```java
// Создаём пустое изображение (гистограмму)
FractalImage image = new FractalImage(width, height);

// Для каждого образца (sample)
for (int sample = 0; sample < iterationCount; sample++) {

    // 1. Случайная начальная точка в [-1, 1]
    Point point = new Point(random.nextDouble() * 2 - 1,
                           random.nextDouble() * 2 - 1);

    // 2. Итерации для этой точки
    for (int step = -20; step < iterationCount; step++) {

        // 2a. Случайная аффинная трансформация
        AffineTransformation affine = randomAffine();
        point = affine.apply(point);

        // 2b. Случайная нелинейная трансформация (с весами)
        Transform transform = selectByWeight();
        point = transform.apply(point);

        // 2c. Если прошли разогрев и точка в границах
        if (step >= 0 && inBounds(point)) {
            // Переводим мировые координаты в пиксели
            int x = worldToPixel(point.x, width);
            int y = worldToPixel(point.y, height);

            // Накапливаем в гистограмме
            image.pixel(x, y).hit(point.r, point.g, point.b);
        }
    }

    // Логируем прогресс
    if (sample % (iterationCount / 10) == 0) {
        log("Progress: " + (sample * 100 / iterationCount) + "%");
    }
}

// 3. Нормализация и сохранение
processor.save(image, outputPath);
```

### Координаты: мир → пиксели

**Мировые координаты:** (-1.5, -1.5) до (1.5, 1.5) - область 3x3

**Пиксели:** (0, 0) до (1920, 1080)

**Преобразование:**

```java
int x = (int) ((point.x - world.x) / world.width * imageWidth);
int y = (int) ((point.y - world.y) / world.height * imageHeight);
```

**Пример:**
- Точка в центре мира: (0, 0)
- → Пиксель: (960, 540) - центр изображения

---

## Влияние параметров

### Что влияет на СКОРОСТЬ?

|           Параметр           |               Влияние               |
|------------------------------|-------------------------------------|
| **Количество итераций**      | Линейное: 2x итераций = 2x время    |
| **Разрешение**               | Почти нет (просто запись в массив)  |
| **Количество трансформаций** | Минимальное                         |
| **Сложность трансформаций**  | Среднее (sin/cos/sqrt медленные)    |
| **Количество потоков**       | Сильное: N потоков = ~N раз быстрее |

### Что влияет на КРАСОТУ?

|        Параметр         |                 Влияние                  |
|-------------------------|------------------------------------------|
| **Seed**                | Полностью меняет фрактал!                |
| **Трансформации**       | Какие формы получатся                    |
| **Веса функций**        | Какие формы доминируют                   |
| **Аффинные параметры**  | Цвета и структура                        |
| **Количество итераций** | Детализация (после 100k почти незаметно) |
| **Количество потоков**  | НЕ ВЛИЯЕТ (результат идентичен)          |

### Что НЕ влияет?

- Скорость генерации
- Количество потоков (на результат)
- Имя файла
- Порядок применения трансформаций (он случайный)

---

## Практические советы

### Для красивых фракталов:

1. **Используй 4-6 разных трансформаций** - больше разнообразия
2. **Комбинируй противоположные:** swirl + spherical, spiral + disc
3. **Один доминант:** дай одной функции вес 2.5-3.0, остальным 0.8-1.5
4. **4-5 аффинных трансформаций** - больше цветов
5. **30,000-50,000 итераций** - золотая середина (качество/скорость)

### Для экспериментов:

- **Меняй seed** - каждый даёт уникальный фрактал
- **Играй с весами** - они сильно меняют вид
- **Пробуй экстремальные аффинные:** a=2.0, e=-1.5, и т.д.

### Для производительности:

- **Используй все ядра:** `-t 8` на 8-ядерном CPU
- **Не больше 100k итераций** - после качество не улучшается
- **1920x1080** - оптимальное разрешение

---

## Заключение

Фрактальное пламя - это **контролируемый хаос**:
- Случайные выборы трансформаций
- Детерминированные вычисления (seed)
- Накопление в гистограмме
- Красивая картинка из миллионов точек

**Главная магия:** при многократных случайных применениях трансформаций точка притягивается к **аттрактору** - устойчивой структуре в пространстве.

Экспериментируй, меняй параметры, создавай уникальные фракталы! 🔥
